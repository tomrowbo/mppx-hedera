[profile.default]
src = "src"
out = "out"
libs = ["lib", "dependencies"]
solc_version = "0.8.26"
optimizer = true
optimizer_runs = 200

[rpc_endpoints]
abstract_testnet = "${ABSTRACT_TESTNET_RPC}"
abstract_mainnet = "${ABSTRACT_MAINNET_RPC}"

[dependencies]
forge-std = "1.15.0"
solady = "0.1.26"
